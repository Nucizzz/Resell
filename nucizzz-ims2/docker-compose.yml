backend:
  build: ./backend
  env_file: .env
  depends_on: [db]
  volumes: [uploads:/app/uploads]
  ports:
    - "8000:8000" # <— aggiungi

frontend:
  build: ./frontend
  env_file: .env
  depends_on: [backend]
  ports:
    - "5173:5173" # <— aggiungi

# caddy:
#   image: caddy:2
#   ...
